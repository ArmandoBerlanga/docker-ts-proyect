# Proyecto base TS con Docker

## Para correr el proyecto
1. Deberas tener instalado docker desktop
2. Correr el comando `docker-compose up -d --build` para produccion
3. Correr el comando `docker-compose -f docker-compose.dev.yml up -d --build` para desarrollo
